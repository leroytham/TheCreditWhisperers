const Navbar = () => {
    return (
      <nav>
        <a href="/">Register</a>
        <a href="/login">Login</a>
        <a href="/dashboard">Dashboard</a>
      </nav>
    );
  };
  
  export default Navbar;
  