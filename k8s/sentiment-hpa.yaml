# =============================================================================
# TheCreditWhisperers - Sentiment Service Horizontal Pod Autoscaler
# =============================================================================
# Automatically scales the sentiment service based on resource utilization.
#
# Features:
# - Scales between 2-8 replicas
# - CPU target: 70% utilization
# - Memory target: 75% utilization (sentiment analysis is memory-intensive)
#
# Usage: kubectl apply -f k8s/sentiment-hpa.yaml

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: creditwhisperers-sentiment-hpa
  namespace: creditwhisperers
  labels:
    app: creditwhisperers
    component: sentiment-autoscaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: creditwhisperers-sentiment

  minReplicas: 2
  maxReplicas: 8

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70

    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75

  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
        - type: Percent
          value: 100
          periodSeconds: 60
      selectPolicy: Max

    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120
      selectPolicy: Min
